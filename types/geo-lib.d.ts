declare module 'geo-lib' {
    export function center(coordinates: { longitude: number; latitude: number }[]): { longitude: number; latitude: number };
    // Add other functions or types if you use more from the library
}